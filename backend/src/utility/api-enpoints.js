const task = {
    task : "/task",
    task : "/task/:id", 
    allTask : "/tasks"
}

const user = {
    user : "/user",
    user : "/user/:id",
    allUser : "/users"
}

module.exports = {
    task,
    user
}